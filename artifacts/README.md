# Artifacts

Large generated files are not stored in Git.

Expected local directories:

- `model-weights/`: trained or downloaded model checkpoints.
- `generated-explanations/`: full batch explanation outputs.
- `selected-explanations/`: curated explanation examples used for analysis or presentation.
- `plots/`: output plots from notebook runs.